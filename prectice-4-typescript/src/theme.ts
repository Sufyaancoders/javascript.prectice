import { createTheme } from "@mui/material"

const themes = createTheme({
  palette: {
    // mode: 'dark',
    primary: { main: '#93aec9ff' },
    secondary: { main: '#9c4262ff' },
  },
  
})

export default themes
